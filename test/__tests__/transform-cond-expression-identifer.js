describe('properly transform style assigned in conditional expression when options are identifiers', () => {
  it('should transform correctly', () => {
    test('test-cond-expression-identifier');
  });
});